/** Curvas de easing canônicas do projeto (Framer Motion). */
export const EASE: [number, number, number, number] = [0.76, 0, 0.24, 1];
/** Expo-out — entradas suaves de painel/mega-menu. */
export const EASE_EXPO: [number, number, number, number] = [0.16, 1, 0.3, 1];
/** Smooth padrão de UI (igual ao ease-in-out do Tailwind). */
export const EASE_SMOOTH: [number, number, number, number] = [0.4, 0, 0.2, 1];
